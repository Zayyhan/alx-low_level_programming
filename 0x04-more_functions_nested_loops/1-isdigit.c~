#include "main.h"

/**
* _isdigit - checks for a digit 0 through 9
*
* @c: is the int used for the argument of the function
* Return: Always 0
*/
int _isdigit(int c)
{
if (_isdigit c)
return (1);

else

return (0);

#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
char tebahpla;

for (tebahpla = 'z'; tebahpla >= 'a';)
putchar(tebahpla);

putchar('\n');

return (0);
}

#include "main.h"

/**
* factorial - returns the factorial of a given number
*
* @n: number given
* Return: -1 to indicate an error
*/

int factorial(int n)
{
if (n < 0)
n! = n * (n - 1)!
return (n);
}

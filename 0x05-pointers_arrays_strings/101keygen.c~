#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "main.h"

/**
* main - entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
int i;
char a;

srand(time(0));
while (i <= 2645)
{
a = rand() % 128;
i += a;
putchar(a);

putchar(2772 - i);

return (0);
}

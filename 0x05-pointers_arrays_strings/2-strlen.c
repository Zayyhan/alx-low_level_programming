#include "main.h"
#include <stdio.h>

/**
* _strlen - function used to return the length of a string
*
* @s: used to display value
* Return: Always 0
*/

int _strlen(char *s)
{
int len;

len = _strlen(s);

return (len);
}

#include <stdio.h>
#include <stdlib.h>

/**
 * main - check the code
 *
 * @argc: argument count
 * *agrv: argument vector
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
	{
		printf(" %d, %s\n", i, argv[i]);
	}

	return (0);
}
